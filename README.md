# Ritumati V11 — Minimal Fix Pack

This is based on the actual V11 `app.js` from the Ritumati GitHub repository.

Only two visual changes were made:
1. Ritumati header logo: 46px → 60px.
2. District photo duplicate/watermark-like names are visually covered by the existing dark image gradient, while the single HTML district name remains visible.

**Do not replace your V11 `index.html`.**
Replace only:
`app.js`

This preserves the V11 layout, hero, mission section, testimonials, gallery, district expansion behavior, Google Sheets integration, and all existing GitHub assets.
